# Use the 'create api' subcommand to add watches to this file.
- group: charts.trustification.io
  version: v1
  kind: TrustedProfileAnalyzer
  chart: helm-charts/redhat-trusted-profile-analyzer
# +kubebuilder:scaffold:watch
